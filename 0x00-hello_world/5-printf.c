#include <stdio.h>

/**
 * main - ' entry point'
 *
 * Description: 'using printf to output str'
 *
 * Return: always 0 (successful)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
